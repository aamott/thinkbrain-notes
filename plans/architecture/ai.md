# AI

AI is deferred beyond MVP.

See `plans/deferred/ai.md` for future direction.

Do not implement AI provider abstractions, ACP (Agent Client Protocol) integration, embeddings, model settings, chat panels, or AI editing features during MVP unless a future work item explicitly assigns them.
