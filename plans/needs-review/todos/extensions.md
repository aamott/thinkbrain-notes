> [!WARNING]
> **AI Synthesized**: This file was synthesized by an AI agent based on conversational context. It was not explicitly written in the final chat summary and requires manual review.

# Extensions TODOs

- [ ] Define the `IExtensionHost` interface in the `shared-types` package.
- [ ] Build the UI sandbox for extensions (allowing views, panels, menus, and editor actions).
- [ ] Build a plugin manager screen (install, uninstall, disable, update).
- [ ] Create the manifest schema (`extension.json`).
- [ ] Expose basic editor commands to the extension host API.
