<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.thinkbrain_notes_desktop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Render into display cutout areas so the webview is truly edge-to-
             edge; the header consumes the inset via env(safe-area-inset-top). -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
