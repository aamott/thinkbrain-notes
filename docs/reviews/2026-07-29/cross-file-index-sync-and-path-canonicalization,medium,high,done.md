- name: Workspace search index synchronization and workspace path canonicalization consistency
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/markdown.rs
- lines: 58-188
- description: 1) `markdown.rs` CRUD commands (`create_markdown_file`, `write_markdown_file`, `rename_markdown_file`, `delete_markdown_file`) perform disk operations without notifying or updating the search index in `search.rs`. Re-indexing relies entirely on frontend IPC orchestration. If an operation succeeds on disk but the frontend IPC fails or is disconnected, the search index (`documents_fts` in `search.rs`) becomes stale or retains deleted documents. Native event notification or internal workspace event broadcasting between `markdown.rs` and `search.rs` would ensure consistent search index state. 2) In `settings.rs`, `recent_workspace_paths` stores un-canonicalized path strings, whereas `search.rs` and `settings.rs` rely on `resolve_workspace_root` for stable workspace hash generation (`stable_workspace_hash`). Normalizing recent workspace paths through `resolve_workspace_root` ensures consistent workspace identity and prevents duplicate cache files or state mismatches. 3) All three files (`markdown.rs`, `search.rs`, `settings.rs`) start with `#![allow(unused)]`, masking dead code across the command handler boundary.
- verification: Code review of markdown.rs lines 58-188, search.rs lines 45-111, and settings.rs lines 206-305.
