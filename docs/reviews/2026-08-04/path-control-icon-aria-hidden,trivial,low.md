- name: PathControl Browse button icon lacks aria-hidden and button relies on text for its accessible name
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src/settings/controls/PathControl.tsx
- lines: 43-52
- description: The Browse button (lines 43-52) contains a `<FolderOpen className="size-3.5" />` icon (line 50) followed by a `<span>Browse</span>` (line 51). The icon has no `aria-hidden="true"`, so some screen readers may announce redundant SVG semantics alongside the "Browse" text. The button's accessible name currently resolves to "Browse" from the text span, which is acceptable, but the icon should be marked decorative for consistency with the rest of the reviewed files (e.g. SettingsContent.tsx line 208 `RotateCcw ... aria-hidden="true"`, SettingsSaveBar.tsx lines 162 and 175 `aria-hidden="true"` on Download/Upload). Additionally the button has a `title` but no `aria-label`; since the text span already provides the name, the `title` is a redundant tooltip — acceptable, but the icon aria-hidden is the actionable item.
- verification: Read PathControl.tsx in full. Confirmed line 50 `<FolderOpen className="size-3.5" />` has no `aria-hidden`. Cross-checked SettingsContent.tsx line 208 and SettingsSaveBar.tsx lines 162/175 which all set `aria-hidden="true"` on lucide icons.
