- name: SettingsContent highlight ring is applied without scrolling the row into view
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src/settings/SettingsContent.tsx
- lines: 104-109, 158-163, 218-235
- description: When a search result is clicked, `SettingsNav.handleResultSelect` (SettingsNav.tsx lines 284-288) calls `setActiveSection(definition.section)` and `requestSettingHighlight(definition.key)`. `SettingsContent` subscribes (lines 161-163) and sets `highlightKey`, which causes the matching `SettingRow` to render with `ring-2 ring-ring bg-accent` (line 108). However, the content area is an independent scroll container (`overflow-y-auto`, line 191), and nothing scrolls the highlighted row into view. If the matched setting is below the fold in a long section (e.g. a section with 20+ settings), the user clicks a search result, the section switches, but the highlighted row is off-screen — the user sees an unhighlighted top of section and may believe the search failed. The `data-setting-key` attribute (line 105) is already in place to locate the row; a `useEffect`/`useLayoutEffect` keyed on `highlightKey` could call `container.querySelector(\`[data-setting-key="${highlightKey}"]\`)?.scrollIntoView({ block: "center" })` when a non-null highlight arrives.
- verification: Read SettingsContent.tsx in full. Confirmed the highlight is purely a CSS class change (line 108) with no scroll logic, and that the content area scrolls independently (line 191 `overflow-y-auto`). Confirmed `data-setting-key` is set on each row (line 105).
