- name: Consolidate bounded authenticated network operations
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/sync/round.rs
- lines: 182-192, 233-243
- description: Fetch and push repeat the same repository, destination, profile, and cancellation cloning before nesting work inside `bounded(network::NETWORK, ...)` and `credentials::with_profile(...)`. Extract one generic helper that owns those wrappers and accepts only the operation-specific closure. Have the fetch caller pass `Arc::clone(&cancel)` and the final push caller move `cancel`; expose the helper's owned cancellation handle to the closure so fetch retains cancellation behavior. This should remove roughly 16 lines and prevent future network operations from accidentally bypassing the selected credential profile. Keep this deferred because the helper's `Send + 'static` bounds and cancellation ownership need focused Rust verification.
- verification: Run `round_tests` and `round_security_tests`, including refused-push retry and cancellation cases, then `pnpm qa`. Confirm fetch leaves the caller's cancellation handle usable and both operations still execute on the network lane under the selected profile.
