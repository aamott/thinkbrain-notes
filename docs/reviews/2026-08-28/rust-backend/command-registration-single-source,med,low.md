- name: Generate command handlers and test metadata from one list
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/mod.rs
- lines: 39-160, 203-276
- description: Every Tauri command is manually repeated in `app_command_handlers!`, `APP_COMMAND_PATHS`, and many presence assertions. The duplicate test list cannot prove that the handler macro matches it; both can drift independently, despite the surrounding comment claiming the test guards that boundary. Define the command paths once in a private list macro, then expand that list into `tauri::generate_handler!` and a `stringify!`-derived test array. Keep only uniqueness and expected-count assertions plus any deliberate platform contract assertions. This removes roughly one full command list and makes registration drift structurally impossible. Defer because macro hygiene around `$crate` paths and `tauri::generate_handler!` should be proven in a focused Rust change rather than mixed into unrelated cleanup.
- verification: Temporarily add and remove a command in the source list to confirm handlers and metadata move together. Run the registration test, `cargo check` for desktop and Android targets where available, then `pnpm qa`.
