- name: Retire or delegate redundant Markdown rename/delete commands
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/markdown.rs
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/workspace_entries.rs
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src/native/commands.ts
- lines: markdown.rs 243-312; workspace_entries.rs 128-259; commands.ts 120-134
- description: `rename_markdown_file` and `delete_markdown_file` duplicate the generic workspace-entry operations, but repo-wide search finds no renderer or extension caller; extensions already route note rename/delete through `rename_workspace_entry` and `delete_workspace_entry`. The generic commands also preserve watcher suppression and search-index removal. This is roughly 85 lines of redundant Rust and command-map/registration surface. Do not remove it as ordinary dead code because Tauri IPC names are an externally callable boundary and the workspace-explorer plan explicitly said to retain them. First decide whether direct IPC compatibility is supported. If not, remove both commands, map entries, registrations, and registry assertions. If compatibility is required, deprecate them and delegate to shared implementations while retaining Markdown path validation and existing error contracts.
- verification: Search for both command names across production, E2E, extension, and documentation code; currently only their definitions, command map/registry, and the plan reference remain. Run Markdown/workspace-entry Rust tests, renderer typecheck, E2E command mocks, and `pnpm qa` after changing the boundary.
