- name: round_tests.rs exceeds 500-line preferred file-size limit
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/sync/round_tests.rs
- lines: 1-694
- description: The file is 694 lines, above the project's "< 500 lines preferred" guidance (AGENTS.md: "Small, focused files (< 500 lines preferred)" and "Avoid large files. Never over 800 lines. Break up earlier rather than later when it is already big."). It is still under the 800 hard cap, but it bundles four logically distinct test groups under one file: destination-settings tests (104-151), happy-path round trips (157-215), merge/conflict behavior (223-434), security/path-escape tests (436-591), and failure/timeout tests (642-694). Splitting the security tests (`a_note_whose_name_cannot_be_read_here_is_refused`, `a_note_named_to_escape_the_folder_is_refused`, plus their `tree_holding`/`commit_of` helpers) into a `round_security_tests.rs` would bring the main file under 500 and co-locate the hostile-history crafting helpers with the only tests that use them.
- verification: Read the full file (694 lines); confirmed line count and that `tree_holding`/`commit_of` are used only by the two security tests at lines 524-543 and 571-591.
