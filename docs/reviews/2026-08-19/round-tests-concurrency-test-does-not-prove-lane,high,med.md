- name: Concurrency test docstring admits it does not prove the behavior under test
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/sync/round_tests.rs
- lines: 460-508
- description: `two_syncs_at_once_on_one_vault_do_not_interleave` has a docstring that explicitly states:
  "This does not prove the lane: removing it leaves the test passing, because whichever thread merges first leaves the rest with nothing to merge, and the collision needs two of them past the fetch before either commits."
  The test name claims to verify that two concurrent syncs on one vault do not interleave, but the docstring concedes the test passes vacuously — removing the very serialization mechanism under test (`lane`) leaves it green. This is a test that does not actually assert the behavior it claims to. The assertions present (all outcomes Ok, remote tip matches local head, content matches) would hold with or without the lane because the workload rarely produces the interleaving the lane is meant to prevent. Either (a) construct a workload that reliably forces two threads past the fetch before either commits (e.g., pre-fetch on all threads, then barrier-release into the merge/commit), or (b) rename the test and adjust the docstring to honestly describe what it does cover (that concurrent syncs do not corrupt history or panic), and add a separate, deterministic test for the lane.
- verification: Read lines 460-508 of round_tests.rs; the docstring at 460-468 explicitly states "This does not prove the lane: removing it leaves the test passing".
