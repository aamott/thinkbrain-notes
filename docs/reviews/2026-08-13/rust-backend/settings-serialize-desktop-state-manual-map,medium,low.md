- name: `serialize_desktop_state` builds a JSON `Map` by hand instead of deriving `Serialize`
- file: /media/adam/extex/projects/thinkbrain-notes/apps/desktop/src-tauri/src/commands/settings.rs
- lines: 673-760
- description: `serialize_desktop_state` (lines 673-746) and `serialize_persisted_tab` (lines 748-760) hand-build a `serde_json::Map<String, Value>` with ~12 `serialized.insert(...)` calls, then wrap in `Value::Object`. `DesktopState` and `PersistedTab` already derive `Deserialize`/`PartialEq`/`Clone`/`Debug`. Deriving `Serialize` with `#[serde(rename_all = "camelCase")]` (matching the existing `Deserialize` derive on `PersistedTab` at line 29 and the implied camelCase shape of `DesktopState`) would let `serde_json::to_value(&state)` produce the same object in ~1 line, replacing ~80 lines of manual insertion. The only field needing custom handling is `workspace_views` (a `BTreeMap<String, BTreeMap<String, Vec<String>>>`) — but that serializes to exactly the nested-object shape the hand-built code produces, so no custom serializer is required.

  The hand-rolled version exists to control key order and the `Value::Null` vs absent-field choice for `lastWorkspacePath`/`activeTabId`. Both are achievable with `#[serde(skip_serializing_if = "Option::is_none")]` (which the codebase already uses on `WorkspaceChange.old_path` in watcher.rs:86) — but note the current code emits `Value::Null` *present* rather than *absent*, so the frontend may depend on the key existing with `null`. If so, keep `Option` without `skip_serializing_if` and `serde_json::to_value` will emit `null` for `None`, matching today's output exactly.

  This is a real compaction win: ~80 lines → ~15 (struct derive + a 2-line `to_value` call), and removes a parallel write schema that can drift from the read schema in `create_desktop_state` (lines 504-548).
- verification: read lines 673-760 (manual map building) and 504-548 (the reader); confirmed `PersistedTab` already derives `Deserialize` with `rename_all = "camelCase"` (line 29) so the camelCase keys match. Grepped `serialize_desktop_state` — only caller is `update_desktop_state_contents` (line 327).
- estimated savings: ~60-70 lines.
