## 2026-07-18 review — deferred

- **medium — Workspace-tree virtualization:** Flatten expanded nodes and virtualize the visible row range (the existing `react-arborist` dependency can be evaluated) only after profiling confirms large visible trees are a bottleneck; preserve folder expansion and accessible navigation.
